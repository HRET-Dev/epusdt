services:
  epusdt:
    image: ghcr.io/hret-dev/epusdt:latest
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./env:/app/.env
    ports:
      - "8000:8000"
